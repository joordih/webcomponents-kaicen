// import { createSlice } from '@reduxjs/toolkit'

// export const overviewSlice = createSlice({
//   name: 'overview',
//   initialState: {
//     lastUpdatedOrders: new Map(),
//     lastCreatedOrders: new Map()
//   },
//   reducers: {
//     setLatestUpdatedOrders: (state, action) => {
//       for (const order of action.payload) {
//         this.lastUpdatedOrders.set(order.id, order)
//       }
//     },
//     setLatestCreatedOrders: (state, action) => {
//       for (const order of action.payload) {
//         this.lastCreatedOrders.set(order.id, order)
//       }
//     }
//   }
// })

// export const {
//   setLatestUpdatedOrders,
//   setLatestCreatedOrders
// } = overviewSlice.actions

// export default overviewSlice.reducer
